name: "{{PROJECT_NAME}}"
description: "{{PROJECT_DESCRIPTION}}"
status: planned # planned | in_progress | paused | completed | cancelled
created_at: "{{CREATED_AT}}"
tracker:
  type: "{{TRACKER_TYPE}}" # github | local | custom
  config:
{{TRACKER_CONFIG}}
